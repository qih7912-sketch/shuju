import numpy as np\nimport matplotlib.pyplot as plt\n\ndef multi_slit_interference(num_slits, distance, wavelength, screen_points, slit_spacing):\n    # Calculate interference pattern from multi-slit setup\n    theta = np.arcsin(np.linspace(-1, 1, screen_points))\n    k = 2 * np.pi / wavelength  # wave number\n    intensity = np.zeros(screen_points)\n    for slit in range(num_slits):\n        # Calculate phase for each slit\n        phase = k * slit_spacing * slit * np.sin(theta)\n        intensity += np.cos(phase)**2  # interference pattern\n    return intensity, theta\n\ndef visualize_pattern(intensity, theta):\n    # Visualize the interference pattern\n    plt.figure(figsize=(10, 6))\n    plt.plot(theta * (180 / np.pi), intensity)\n    plt.title('Multi-Slit Interference Pattern')\n    plt.xlabel('Angle (degrees)')\n    plt.ylabel('Intensity')\n    plt.grid()\n    plt.show()\n\ndef calculate_and_visualize(num_slits, distance=1.0, wavelength=0.5, screen_points=1000, slit_spacing=0.1):\n    intensity, theta = multi_slit_interference(num_slits, distance, wavelength, screen_points, slit_spacing)\n    visualize_pattern(intensity, theta)\n\n# Example usage for 2 to 5 slits\nfor slits in range(2, 6):\n    calculate_and_visualize(slits)